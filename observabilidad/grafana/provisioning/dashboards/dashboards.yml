apiVersion: 1

providers:
  - name: peluqueria
    type: file
    # Deja editar el dashboard en la interfaz para probar cosas, pero la fuente de verdad es
    # el JSON del repositorio: al reiniciar, vuelve a lo que diga el fichero.
    allowUiUpdates: true
    options:
      path: /var/lib/grafana/dashboards
      foldersFromFilesStructure: false
