# Perfil de desarrollo

# El schema lo gestiona Flyway (migraciones versionadas). Hibernate solo valida que
# las entidades coincidan con las tablas creadas por Flyway; no modifica la BD.
spring.jpa.hibernate.ddl-auto=validate

# Imprime las queries SQL en consola (util para debug)
spring.jpa.show-sql=true
